# Implementation Plans

Generated by the improve skill on 2026-06-14 (against commit `557eaf4f`).
Execute in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title                                                              | Priority | Effort | Depends on | Status                                                                          |
| ---- | ------------------------------------------------------------------ | -------- | ------ | ---------- | ------------------------------------------------------------------------------- |
| 001  | Add a CI pipeline (lint/types/format/build/registry validate)      | P1       | M      | —          | DONE on `staging` (staged) — 2 follow-ups before CI is green (see Verification) |
| 002  | Establish Vitest + characterization tests for pure logic           | P1       | M      | —          | DONE on `staging` (commit `dfe611e0`) — verified 14/14 tests, lint clean        |
| 003  | Keep the registry `Index` out of the client bundle                 | P2       | M      | 002 (rec.) | TODO                                                                            |
| 004  | Harden external data fetches against non-OK responses              | P2       | S      | —          | TODO                                                                            |
| 005  | Reduce dependency advisories (+ remove unused `react-use`)         | P2       | S–M    | 001 (rec.) | TODO                                                                            |
| 006  | Quick wins: escape RSS XML, guard feed dates, doc generated mirror | P3       | S      | —          | TODO                                                                            |

## Execution log (2026-06-14)

**Environment issue affecting all `execute` runs**: the worktree isolation created
both executors' worktrees from `origin/main` (`5cca24c6`), **not** from the
current `staging` HEAD (`557eaf4f`) that these plans target. `main` and `staging`
have diverged (common ancestor `acfdcfa2`); on `main`, `package.json` reads
`pnpm@11.1.1` / `build: "next build"` while `staging` reads `pnpm@11.5.3` /
`build: "pnpm registry:build && next build"`. Resolve this before further
`execute` runs — otherwise produced branches can't be cleanly merged into `staging`.

**002** — Executor COMPLETE. Added `vitest@4.1.8`, `test`/`test:run` scripts,
`vitest.config.ts`, and three test files (`url`, `registry`, `findNeighbour`);
14 tests pass, `check-types` and `lint` clean (reviewer re-verified in the
worktree). The tested symbols (`findNeighbour`, `registryConfig`) were confirmed
**identical between `main` and `staging`**, so the tests are valid on `staging`.
The executor's package.json delta is purely additive (2 scripts + 1 devDep).

- Branch: `advisor/002-test-baseline` (commit `86dcbc91`), based on `main`.
- Worktree: `.claude/worktrees/agent-a25677698bb098309`.
- **To land on `staging`**: cherry-pick `86dcbc91` onto `staging`, then run
  `pnpm install` to regenerate `pnpm-lock.yaml` against staging's deps. Do NOT
  merge the branch wholesale (it would drag in `main`'s divergence).
- Verdict: APPROVED on merit; status stays short of plain DONE until landed on `staging`.

**001** — Executor STOPPED on two legitimate, real defects (valid on `staging` too);
the plan has been refined to fix both:

1. `pnpm check-types` needs `.next/types` (the Next-generated `PageProps` global),
   so **Build must run before Typecheck**. The workflow + local steps were reordered.
2. `pnpm build` prerenders `/`, which calls the GitHub-contributions fetcher;
   `GITHUB_CONTRIBUTIONS_API_URL` has no in-code fallback, so an unset value fails
   the build. A workflow `env:` block now supplies the public (non-secret) URL.
   (The executor's "pnpm 11.1.1 / different build script" report was a wrong-base
   artifact — those are `main`'s values; the plan correctly targets `staging`'s
   `11.5.3`. A STOP condition now catches the wrong-base case.)

- No files were committed by the 001 executor (it stopped before writing).
- Ready to re-execute once the worktree-base issue above is resolved.

## Verification (2026-06-14, on `staging` after manual execution)

Both plans were applied manually on `staging` (002 committed as `dfe611e0`; 001
files staged: `.github/workflows/ci.yml`, `DEVELOPMENT.md`, plus `.nvmrc` bumped
to `24.16.0`).

**002 — VERIFIED DONE.** `pnpm test:run` → 3 files / 14 tests pass; `pnpm lint` → 0.
Test infra (`vitest@4.1.8`, `test`/`test:run` scripts, `vitest.config.ts`, 3 test
files) all present.

**001 — `ci.yml` is correct** (env block + build-before-typecheck order verified).
Two follow-ups needed before the first CI run is green:

1. `pnpm format:check` fails on the 7 `plans/*.md` files (now tracked). Fix: add
   `plans` to `.prettierignore` (advisor docs shouldn't be gated by the code
   formatter), or run Prettier on them.
2. `DEVELOPMENT.md` "Before pushing" lists `pnpm check-types` before `pnpm build`;
   reorder to build-first to match `ci.yml` (check-types needs `.next/types`).

Non-issue: a local `pnpm check-types` reported one error referencing the untracked
`src/app/(98cn)/` experiment via a stale `.next/types/validator.ts`. Not a code
defect and not present in CI (the route isn't committed; CI rebuilds `.next/types`).

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Recommended sequencing

1. **001 + 002 first** — they are the unblockers. CI (001) makes every later
   plan's "the build passes" an enforced fact; the test runner (002) gives the
   later refactors a safety net. After 002 lands, add a `pnpm test:run` step to
   the 001 workflow.
2. **004 and 006** — small, low-risk, independent; can go anytime (good
   warm-ups, and they exercise the new CI).
3. **005** — dependency hygiene; best after 001 so CI re-runs the audit/build.
4. **003 last of this set** — the only MED-risk change; it is measure-gated and
   benefits from having the test/CI loop in place.

## Dependency notes

- 002 → 001: not a hard dependency, but once 002 adds a test runner, the 001
  workflow should gain a test step. Land 001 first, then amend it in 002.
- 003 recommends 002 so the executor has a working verify loop; 003 is itself
  measure-gated (Step 0 / Step 3) and will STOP if the bundle impact is negligible.
- 004 regenerates `src/registry/transformed/**` and `public/r/**` via
  `pnpm registry:build` — those generated changes are expected in its diff.

## Findings considered and rejected (so they are not re-audited)

- **ClipboardItem given `Promise<string>`** (doc-page-actions.tsx): NOT a bug —
  `Promise<string>` is valid per the Clipboard API spec and the TS DOM types.
- **"GitHub contributions promise not awaited"** (github-contributions/index.tsx):
  NOT a bug — it is the intended React 19 `use()` streaming pattern
  (`graph.tsx:27` calls `use(contributions)` inside a `<Suspense>` boundary).
- **Committed secret in `.env.local`**: NOT exposed — `.env.local` is gitignored
  (`.env*`) and untracked.
- **`src/registry/transformed/` duplication**: NOT debt — it is an autogenerated
  mirror. (We do, however, document it as never-edit in plan 006.)
- **`console.log` in `events.ts`**: NOT a prod issue — stripped by
  `removeConsole` in `next.config.ts` (production excludes only `error`).
- **De-dupe the two GitHub-contributions fetchers** (originally bundled into the
  #4 finding): rejected — they query different shapes (last-year graph vs.
  multi-year 480-cell grid) with different cache keys; merging would couple two
  unrelated concerns. Plan 004 keeps them separate and fixes error handling plus
  the missing URL fallback (so the build no longer needs `GITHUB_CONTRIBUTIONS_API_URL`).

## Open product questions left to the maintainer (no plans written)

- **`(98cn)` Windows-98 UI kit** — half-built experiment (13 components, 2 TODOs,
  a `/98cn` page). Finish + document, or remove. Needs an intent decision.
- **Dual analytics** — both PostHog and OpenPanel fire on every `trackEvent`
  (`src/lib/events.ts:45-46`). If one is legacy, dropping it removes a client dep.
  Needs to know which is the source of truth.

## Lower-value findings noted but not planned

- `insights.ts` uses `!` non-null assertions on OpenPanel secrets; if missing it
  fails silently (already caught, returns null). Cosmetic.
- `src/lib/registry.ts` LRU TTL is 5 min in production (small re-read cost on a
  static, deploy-time-rebuilt registry). Low impact.
- `src/lib/remark-component.ts` rewrites imports with regex (explicit TODO to use
  `@swc/core`). Input is controlled registry code; low urgency — revisit if the
  registry grows or a mis-rewrite is observed.
